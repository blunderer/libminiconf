
#
# LIBMINICONF
#

libminiconf is a config parser for simple config files based on macro.
One can easily open a configuration file and retrieve the value of a key.
One can also iterate thru key/value pairs.

libminiconf handle multiple config file by assigning an ID.

